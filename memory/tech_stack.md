# Tech Stack

- Node.js build scripts (`scripts/build-html.js`, `scripts/validate.js`)
- Data schema: `schema/vocab.yml` + `schema/entry.schema.json`
- Static HTML output in `_site/`
- Markdown source: `README.md` (primary content)
- Git, GitHub (branch `pub-plan`, main `main`)

(update when new tools/frameworks added)
